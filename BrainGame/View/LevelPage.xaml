<Page
    x:Class="BrainGame.View.LevelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrainGame.View"
    xmlns:viewModels="using:ViewModels"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <GridView ItemsSource="{x:Bind fieldLevelViewModel.Levels, Mode=OneWay}" SelectedIndex="{x:Bind fieldLevelViewModel.SelectedIndex, Mode=TwoWay}">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="5">
                        <TextBlock Text="{Binding Raiting}"></TextBlock>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
